# Distribution (MIGRATION_PLAN.md phases 9–10)

How LP-OS ships: web/PWA, native desktop bundles via `deno desktop`, Android
TWA, and the retire/redirect checklist for the old repos. Built and verified
2026-07-05 on Deno 2.9.1.

## PWA

Both apps are installable PWAs with offline fallback.

- **apps/shell** — `static/manifest.webmanifest` + `static/sw.js`, linked and
  registered by the OS shell page (`renderOSShell` in `main.ts`). The service
  worker is network-first everywhere (the server marks responses
  no-cache/no-store; the SW cache is a last-known-good offline fallback, never a
  freshness layer) and never intercepts `/api/*`, `/gelf`, or `/health`. Bump
  `SW_VERSION` in `sw.js` to invalidate cached copies after a deploy.
- **apps/member** — `static/manifest.webmanifest` + SvelteKit's
  `src/service-worker.ts` (bundled and auto-registered by SvelteKit; its cache
  key includes the build version, so every deploy invalidates itself). Vite
  build output is served cache-first, everything else network-first.
  - `scripts/patch-deploy.mjs` runs after every build: @deno/svelte-adapter
    0.2.x enumerates `static/` into `.deno-deploy/deploy.json` but misses the
    Vite-built `service-worker.js`, which would otherwise 404 and never install.
    Remove the patch once the adapter fixes this.
- **Icons** — the shared LP-OS diamond mark: orange on `#0b0d11` (shell), yellow
  on `#1a1916` (member). `icon.svg` is hand-authored in each app's
  `static/icons/`; the PNGs (192/512/512-maskable) and Windows `icon.ico` are
  generated by `deno task gen:icons` (`scripts/gen-icons.ts`, no image deps).
- The tracker's manifest extras (`share_target`, `web+lptracker` protocol
  handler) belong to the scanner app and migrate with tiktok-sample-tracker, not
  with these two apps.

## Desktop binaries (`deno desktop`)

Requires **Deno 2.9+** (`deno upgrade`); the desktop subcommand does not exist
in 2.7/2.8. Experimental upstream — expect churn.

- `deno task desktop:shell` → `dist/LP-OS/`
- `deno task desktop:member` → `dist/LP-OS-Member/`
- `deno task desktop:shell:cef` → `dist/LP-OS-cef/`, `--backend cef` (bundled
  Chromium, ~400MB on disk vs ~90MB webview) — the backend that renders on
  Windows-on-ARM, see known issues.
- `deno task desktop:shell:raw` → same output as `desktop:shell`,
  `--backend raw` (bare winit window host — no web engine, so the window is
  blank; only useful to prove the compiled payload boots and serves).

Flag-position gotcha: `deno desktop` treats everything after the entrypoint as
*script* args, so `deno task desktop:shell --backend cef` silently builds the
default webview backend (the shell task ends in `main.ts`); the member task has
no entrypoint, so `deno task desktop:member --backend cef` does work.

On Windows each bundle is a directory: `<Name>.bat` launcher,
`laufey_webview.exe` (the webview host), and `<Name>.dll` (the compiled app
payload). The desktop runtime overrides the server's listen port and points the
native window at whatever port it bound — `PORT` is ignored in the desktop
build.

Why the task flags are what they are:

- `--node-modules-dir=none --no-lock` (both apps): without these, the compile
  embeds the entire workspace npm snapshot from the shared lockfile — including
  apps/member's whole Vite/Svelte toolchain — bloating the shell payload from
  ~7MB to ~208MB. With them, only the entrypoint's actual npm graph embeds
  (resolved from the global cache).
- `--include static` (shell): the shell reads `static/` at runtime via
  `Deno.readFile`; compile only embeds the module graph unless told otherwise.
- `build:scan-client` prebuild (shell): `/scan-client.js` is normally bundled on
  demand by spawning `deno bundle`, which a compiled binary can't do
  (`Deno.execPath()` is the app itself). The prebuilt `static/scan-client.js`
  (gitignored) is served by the static middleware instead. Side effect: while
  that file exists locally, dev serves it too — delete it if you're editing
  `packages/relay/client.ts` and testing in dev.
- `--no-check` (member): the adapter's generated `.deno-deploy/` output does not
  typecheck; `npm run check` (svelte-check) covers the real sources.
- App name/identifier/icons/output live in each app's `deno.json` `"desktop"`
  block. Cross-compile with `--target <triple>` or `--all-targets` (linux
  x64/arm64, windows x64, mac x64/arm64 — **no Windows ARM64 target exists**).

### Known issues (deno 2.9.1 / laufey 0.5.0, 2026-07-05)

- **Windows on ARM:** the x64 `laufey_webview.exe` host crashes at window
  creation under emulation (0xc0000409 in ucrtbase.dll; WebView2 is installed
  and current). The compiled server inside the payload works — verified by
  probing its port before teardown. Fix verified on this machine 2026-07-06:
  build with the CEF backend (`deno task desktop:shell:cef`) — the bundled
  x64 Chromium runs fine under emulation and renders the shell UI. `--backend
  raw` also stays up, but it's a bare winit host with no web engine (blank
  window), so it only proves the payload boots. Webview bundles are fine on
  x64 Windows.
- **Member desktop on Windows:** requests 500 because the deploy.json static
  routes (`/_app/immutable/:file*`) are opened as literal paths in the desktop
  VFS runtime (os error 123) instead of pattern-matching. The same
  `.deno-deploy` output serves correctly via a plain
  `deno run -A .deno-deploy/server.ts`, so this is upstream
  (@deno/experimental-route-config under `deno desktop`), not our config.
  Re-test on newer Deno releases.
- On this dev machine the x64 Deno and ARM64 Node toolchains fight over
  `apps/member/node_modules`: a bare `deno install` at the repo root replaces
  npm's ARM64 rollup bindings with x64 ones and `npm run build` then dies with
  `MODULE_NOT_FOUND` on rollup natives. Fix: re-run `npm ci` in `apps/member`.

## Android (TWA)

Unchanged this phase: Bubblewrap/TWA wraps the scanner PWA and lives in
tiktok-sample-tracker; it carries forward when that app migrates into LP-OS.
`deno desktop` does not target Android — TWA remains the Android path.

## Phase 10 — retire/redirect checklist (external, manual)

None of these can be done from this repo; they're console/DNS/repo-admin steps.

- [ ] **Before 2026-07-20:** confirm in console.deno.com that data-pimp's deploy
      (its `deno.json` uses the new `{org, app}` format) is actually on the new
      Deno Deploy platform — Deploy Classic shuts down that day.
- [ ] Deploy apps/shell and apps/member to the new Deno Deploy (add `deploy`
      blocks once org/app names and the production domain are decided; the
      plan's target: shell replaces data-pimp's deploy).
- [ ] Repoint or retire `thirsty.store` / `admin.thirsty.store` once LP-OS is
      verified at parity; then update the shell FOLDERS defaults
      (`INVENTORY_APP_URL` et al.), the extension's default GELF endpoint
      (`extension/config.js`, currently `http://localhost:8000/gelf`), and the
      skills' `LPOS_API_URL` notes.
- [ ] Archive data-pimp, tok-scrape, and tiktok-sample-tracker (READMEs pointing
      here). tok-scrape's `graylog-shim` cutover proceeds on its own schedule —
      its extension/mobile clients are not LP-OS clients.
